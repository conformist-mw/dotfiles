# Managed by Ansible (roles/ha_config). Do NOT edit on the server —
# change the values in group_vars/all/secrets.sops.yaml and re-run `just deploy-ha`.
#
# Only real credentials belong here, and only the git-owned layer may reference
# them: !secret in automations.yaml / scripts.yaml breaks the UI editor and the
# config REST API. Chat ids and config entry ids are identifiers, not secrets —
# they are inlined at the call site.

# --- recorder ---
recorder_db_url: {{ ha_recorder_db_url }}
